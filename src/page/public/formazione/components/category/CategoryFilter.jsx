const CategoryFilter = () => {

  // const Online = data.filter((data) => data.Online === "Online courses");
  // const e_Learning = data.filter((data) => data.eLearning === "e-Learning platform" );
  // const Corporate = data.filter((data) => data.Corporate === "Corporate training" );

  return <div>CategoryFilter</div>;
};

export default CategoryFilter;
